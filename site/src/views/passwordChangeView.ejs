<!-- Trayendo el head -->
<%- include('partials/head', {title: 'Registrarme'}) %>
<body>
    <!-- Trayendo el header -->
    <%- include('partials/header') %>

    <section class="contenedor-gral todo">
        <h1>Cambiar contraseña</h1>
        <div class="cont-info2">
            <form class="cont-boton" action="/users/edit/<%= user.id %>/password?_method=PUT" method="POST">
                <% if (typeof mensaje[0] != 'null') { %>
                    <p><%= mensaje %> </p>
                <% } %> 
                <div class="cont-edit cont-data">
                    <p class="preg">Contraseña previa: </p>
                    <input type="password" class="resp edit"  name="oldPassword">  
                </div>
                <div class="cont-edit cont-data">
                    <p class="preg">Contraseña nueva: </p>
                    <input type="password" class="resp edit" name="password">    
                </div>
                <button type="submit" class="boton">Cambiar Contraseña</button>
            </form>
        </div>
    </section>
    
</body>